/**
 * 应用常量定义
 */

// 分页默认值
export const PAGINATION = {
  DEFAULT_PAGE: 1,
  DEFAULT_PAGE_SIZE: 10,
  MAX_PAGE_SIZE: 100,
} as const;

// 应用配置
export const APP_CONFIG = {
  NAME: "One Creation",
  DESCRIPTION: "AI 创作平台",
} as const;
